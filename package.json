{
  "name": "susan-g-enterprises",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && (prisma migrate deploy || echo 'Skipping migrations — DATABASE_URL not set yet') && next build",
    "start": "next start -p ${PORT:-3000}",
    "lint": "next lint",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "next": "^14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "stripe": "^16.8.0",
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.35",
    "prisma": "^5.20.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
